<script>
    
VirtualSelect.init({
    ele: '#classid11',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid12',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid13',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid14',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid15',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid16',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid17',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid18',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid19',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid110',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid21',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid22',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid23',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid24',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid25',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid26',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid27',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid28',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid29',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid210',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid31',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid32',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid33',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid34',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid35',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid36',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid37',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid38',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid39',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid310',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid41',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid42',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid43',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid44',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid45',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid46',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid47',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid48',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid49',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid410',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid51',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid52',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid53',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid54',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid55',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid56',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid57',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid58',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid59',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid510',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid61',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid62',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid63',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid64',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid65',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid66',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid67',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid68',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid69',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid610',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid71',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid72',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid73',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid74',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid75',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid76',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid77',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});

VirtualSelect.init({
    ele: '#classid78',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid79',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
VirtualSelect.init({
    ele: '#classid710',
    multiple: true,
    search:false,
    textDirection: 'rtl',
    disableSelectAll: true,
        disabledOptions:[1]
});
document.getElementById('sel_row_level11').addEventListener('change', function(e) {
    document.querySelector('#classid11').destroy();
    VirtualSelect.init({
      ele: "#classid11",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid11').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level12').addEventListener('change', function(e) {
    document.querySelector('#classid12').destroy();
    VirtualSelect.init({
      ele: "#classid12",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid12').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level13').addEventListener('change', function(e) {
    document.querySelector('#classid13').destroy();
    VirtualSelect.init({
      ele: "#classid13",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid13').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level14').addEventListener('change', function(e) {
    document.querySelector('#classid14').destroy();
    VirtualSelect.init({
      ele: "#classid14",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid14').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level15').addEventListener('change', function(e) {
    document.querySelector('#classid15').destroy();
    VirtualSelect.init({
      ele: "#classid15",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid15').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level16').addEventListener('change', function(e) {
    document.querySelector('#classid16').destroy();
    VirtualSelect.init({
      ele: "#classid16",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid16').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level17').addEventListener('change', function(e) {
    document.querySelector('#classid17').destroy();
    VirtualSelect.init({
      ele: "#classid17",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid17').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level18').addEventListener('change', function(e) {
    document.querySelector('#classid18').destroy();
    VirtualSelect.init({
      ele: "#classid18",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid18').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level19').addEventListener('change', function(e) {
    document.querySelector('#classid19').destroy();
    VirtualSelect.init({
      ele: "#classid19",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid19').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level110').addEventListener('change', function(e) {
    document.querySelector('#classid110').destroy();
    VirtualSelect.init({
      ele: "#classid110",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid110').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level21').addEventListener('change', function(e) {
    document.querySelector('#classid21').destroy();
    VirtualSelect.init({
      ele: "#classid21",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid21').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level22').addEventListener('change', function(e) {
    document.querySelector('#classid22').destroy();
    VirtualSelect.init({
      ele: "#classid22",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid22').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level23').addEventListener('change', function(e) {
    document.querySelector('#classid23').destroy();
    VirtualSelect.init({
      ele: "#classid23",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid23').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level24').addEventListener('change', function(e) {
    document.querySelector('#classid24').destroy();
    VirtualSelect.init({
      ele: "#classid24",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid24').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level25').addEventListener('change', function(e) {
    document.querySelector('#classid25').destroy();
    VirtualSelect.init({
      ele: "#classid25",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid25').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level26').addEventListener('change', function(e) {
    document.querySelector('#classid26').destroy();
    VirtualSelect.init({
      ele: "#classid26",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid26').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level27').addEventListener('change', function(e) {
    document.querySelector('#classid27').destroy();
    VirtualSelect.init({
      ele: "#classid27",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid27').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level28').addEventListener('change', function(e) {
    document.querySelector('#classid28').destroy();
    VirtualSelect.init({
      ele: "#classid28",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid28').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level29').addEventListener('change', function(e) {
    document.querySelector('#classid29').destroy();
    VirtualSelect.init({
      ele: "#classid29",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid29').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level210').addEventListener('change', function(e) {
    document.querySelector('#classid210').destroy();
    VirtualSelect.init({
      ele: "#classid210",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid210').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level31').addEventListener('change', function(e) {
    document.querySelector('#classid31').destroy();
    VirtualSelect.init({
      ele: "#classid31",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid31').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level32').addEventListener('change', function(e) {
    document.querySelector('#classid32').destroy();
    VirtualSelect.init({
      ele: "#classid32",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid32').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level33').addEventListener('change', function(e) {
    document.querySelector('#classid33').destroy();
    VirtualSelect.init({
      ele: "#classid33",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid33').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level34').addEventListener('change', function(e) {
    document.querySelector('#classid34').destroy();
    VirtualSelect.init({
      ele: "#classid34",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid34').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level35').addEventListener('change', function(e) {
    document.querySelector('#classid35').destroy();
    VirtualSelect.init({
      ele: "#classid35",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid35').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level36').addEventListener('change', function(e) {
    document.querySelector('#classid36').destroy();
    VirtualSelect.init({
      ele: "#classid36",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid36').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level37').addEventListener('change', function(e) {
    document.querySelector('#classid37').destroy();
    VirtualSelect.init({
      ele: "#classid37",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid37').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level38').addEventListener('change', function(e) {
    document.querySelector('#classid38').destroy();
    VirtualSelect.init({
      ele: "#classid38",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid38').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level39').addEventListener('change', function(e) {
    document.querySelector('#classid39').destroy();
    VirtualSelect.init({
      ele: "#classid39",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid39').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level310').addEventListener('change', function(e) {
    document.querySelector('#classid310').destroy();
    VirtualSelect.init({
      ele: "#classid310",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid310').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level41').addEventListener('change', function(e) {
    document.querySelector('#classid41').destroy();
    VirtualSelect.init({
      ele: "#classid41",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid41').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level42').addEventListener('change', function(e) {
    document.querySelector('#classid42').destroy();
    VirtualSelect.init({
      ele: "#classid42",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid42').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level43').addEventListener('change', function(e) {
    document.querySelector('#classid43').destroy();
    VirtualSelect.init({
      ele: "#classid43",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid43').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level44').addEventListener('change', function(e) {
    document.querySelector('#classid44').destroy();
    VirtualSelect.init({
      ele: "#classid44",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid44').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level45').addEventListener('change', function(e) {
    document.querySelector('#classid45').destroy();
    VirtualSelect.init({
      ele: "#classid45",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid45').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level46').addEventListener('change', function(e) {
    document.querySelector('#classid46').destroy();
    VirtualSelect.init({
      ele: "#classid46",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid46').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level47').addEventListener('change', function(e) {
    document.querySelector('#classid47').destroy();
    VirtualSelect.init({
      ele: "#classid47",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid47').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level48').addEventListener('change', function(e) {
    document.querySelector('#classid48').destroy();
    VirtualSelect.init({
      ele: "#classid48",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid48').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level49').addEventListener('change', function(e) {
    document.querySelector('#classid49').destroy();
    VirtualSelect.init({
      ele: "#classid49",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid49').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level410').addEventListener('change', function(e) {
    document.querySelector('#classid410').destroy();
    VirtualSelect.init({
      ele: "#classid410",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid410').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});


document.getElementById('sel_row_level51').addEventListener('change', function(e) {
    document.querySelector('#classid51').destroy();
    VirtualSelect.init({
      ele: "#classid51",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid51').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level52').addEventListener('change', function(e) {
    document.querySelector('#classid52').destroy();
    VirtualSelect.init({
      ele: "#classid52",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid52').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level53').addEventListener('change', function(e) {
    document.querySelector('#classid53').destroy();
    VirtualSelect.init({
      ele: "#classid53",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid53').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level54').addEventListener('change', function(e) {
    document.querySelector('#classid54').destroy();
    VirtualSelect.init({
      ele: "#classid54",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid54').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level55').addEventListener('change', function(e) {
    document.querySelector('#classid55').destroy();
    VirtualSelect.init({
      ele: "#classid55",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid55').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level56').addEventListener('change', function(e) {
    document.querySelector('#classid56').destroy();
    VirtualSelect.init({
      ele: "#classid56",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid56').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level57').addEventListener('change', function(e) {
    document.querySelector('#classid57').destroy();
    VirtualSelect.init({
      ele: "#classid57",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid57').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level58').addEventListener('change', function(e) {
    document.querySelector('#classid58').destroy();
    VirtualSelect.init({
      ele: "#classid58",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid58').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});


document.getElementById('sel_row_level59').addEventListener('change', function(e) {
    document.querySelector('#classid59').destroy();
    VirtualSelect.init({
      ele: "#classid59",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid59').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level510').addEventListener('change', function(e) {
    document.querySelector('#classid510').destroy();
    VirtualSelect.init({
      ele: "#classid510",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid510').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level61').addEventListener('change', function(e) {
    document.querySelector('#classid61').destroy();
    VirtualSelect.init({
      ele: "#classid61",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid61').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level62').addEventListener('change', function(e) {
    document.querySelector('#classid62').destroy();
    VirtualSelect.init({
      ele: "#classid62",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid62').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level63').addEventListener('change', function(e) {
    document.querySelector('#classid63').destroy();
    VirtualSelect.init({
      ele: "#classid63",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid63').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level64').addEventListener('change', function(e) {
    document.querySelector('#classid64').destroy();
    VirtualSelect.init({
      ele: "#classid64",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid64').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level65').addEventListener('change', function(e) {
    document.querySelector('#classid65').destroy();
    VirtualSelect.init({
      ele: "#classid65",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid65').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level66').addEventListener('change', function(e) {
    document.querySelector('#classid66').destroy();
    VirtualSelect.init({
      ele: "#classid66",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid66').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level67').addEventListener('change', function(e) {
    document.querySelector('#classid67').destroy();
    VirtualSelect.init({
      ele: "#classid67",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid67').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level68').addEventListener('change', function(e) {
    document.querySelector('#classid68').destroy();
    VirtualSelect.init({
      ele: "#classid68",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid68').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});


document.getElementById('sel_row_level69').addEventListener('change', function(e) {
    document.querySelector('#classid69').destroy();
    VirtualSelect.init({
      ele: "#classid69",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid69').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level610').addEventListener('change', function(e) {
    document.querySelector('#classid610').destroy();
    VirtualSelect.init({
      ele: "#classid610",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid610').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level71').addEventListener('change', function(e) {
    document.querySelector('#classid71').destroy();
    VirtualSelect.init({
      ele: "#classid71",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid71').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level72').addEventListener('change', function(e) {
    document.querySelector('#classid72').destroy();
    VirtualSelect.init({
      ele: "#classid72",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid72').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level73').addEventListener('change', function(e) {
    document.querySelector('#classid73').destroy();
    VirtualSelect.init({
      ele: "#classid73",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid73').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level74').addEventListener('change', function(e) {
    document.querySelector('#classid74').destroy();
    VirtualSelect.init({
      ele: "#classid74",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid74').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level75').addEventListener('change', function(e) {
    document.querySelector('#classid75').destroy();
    VirtualSelect.init({
      ele: "#classid75",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid75').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level76').addEventListener('change', function(e) {
    document.querySelector('#classid76').destroy();
    VirtualSelect.init({
      ele: "#classid76",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid76').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level77').addEventListener('change', function(e) {
    document.querySelector('#classid77').destroy();
    VirtualSelect.init({
      ele: "#classid77",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid77').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
document.getElementById('sel_row_level78').addEventListener('change', function(e) {
    document.querySelector('#classid78').destroy();
    VirtualSelect.init({
      ele: "#classid78",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid78').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});


document.getElementById('sel_row_level79').addEventListener('change', function(e) {
    document.querySelector('#classid79').destroy();
    VirtualSelect.init({
      ele: "#classid79",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid79').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});

document.getElementById('sel_row_level710').addEventListener('change', function(e) {
    document.querySelector('#classid710').destroy();
    VirtualSelect.init({
      ele: "#classid710",
        multiple: true,
        search:false,
        textDirection: 'rtl',
        disableSelectAll: true,
        disabledOptions:[1]
    });
    let value =this.value;
    let dataUrl = '<?php echo site_url();?>' + '/emp/class_table/getclass_based_student/'+value;
    console.log(dataUrl)
    if(e.target.value.length > 0){
        fetch(dataUrl)
        .then(response => {
            if (!response.ok) {
                throw new Error(`HTTP error! Status: ${response.status}`);
            }
            return response.json();
        })
        .then(data => {
             data.GetClass.forEach(select => {
                document.querySelector('#classid710').addOption({
                    value: select.ClassID,
                    label: select.LevelName + '--' +  select.RowName + '--' + select.ClassName
                });
            });
            
        })
        .catch(error => {
            console.error('Error:', error);
        });
    }
});
</script>